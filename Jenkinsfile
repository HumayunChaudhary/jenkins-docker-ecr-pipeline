pipeline {
    agent any
    stages {
        stage ('Test stage') {
            steps {
                sh '''
                    echo "Welcome to Jenkinsfile" > testfile.txt
                '''
            }
        }
    }
}
